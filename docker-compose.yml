# TODO: Replace this placeholder with the real Umbrel app service definitions.
# Umbrel may merge additional services (for example app_proxy) at install time.
#
# Keep this file syntactically valid so `docker compose config` passes in CI.

services:
  placeholder:
    image: alpine:3.20
    command: ["sh", "-c", "echo 'TODO: replace placeholder with OpenCode app' && exit 0"]
    restart: "no"
